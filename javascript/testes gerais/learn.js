let fruits = ["apple", "orange", "banana", "coconut"];

// fruits.push("pear"); para adicionar ao final
// fruits.pop(); para excluir o último
// fruits.unshift("mango"); para adicionar no início
// fruits.shift(); para excluir o primeiro
// fruits.sort();  para ordenar
// fruits.reverse(); para inverter a ordem

for(let i = fruits.length - 1; i >= 0; i--){
    console.log(fruits[i]);
}